name: Docker ASCII CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Build imagem Docker
        run: docker build -t ascii-app .

      - name: Rodar container
        run: docker run ascii-app